experiment:
  name: unet_hu_dice_focal
  seed: 42

data:
  root_dir: "C:/Users/Hyojun Park/Paper/ct-segmentation/src/content/data"           # contains train/ val/ test/ sub‑dirs
  threshold: 0.3

train:
  batch_size: 4
  epochs: 10
  lr: 0.0001
  loss: dice_focal                  # fixed combo loss
  save_dir: outputs/unet_hu_dice_focal
  num_workers: 2

eval:
  visualize: true