model:
  name: transunet
  use_pretrained: true
  img_size: 224

experiment:
  name: transunet_hu_dice_focal_epoch20_pretrained
  seed: 42

data:
  root_dir: "C:/Users/Hyojun Park/Paper/ct-segmentation/src/content/data"           # contains train/ val/ test/ sub‑dirs
  threshold: 0.3

train:
  batch_size: 4
  epochs: 20
  lr: 0.0001
  loss: dice_focal                  # fixed combo loss
  save_dir: outputs/transunet_hu_dice_focal_epoch20_pretrained
  num_workers: 0

eval:
  visualize: true